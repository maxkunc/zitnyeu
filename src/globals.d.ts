// True only in the static GitHub Pages build (see vite.pages.config.ts); false everywhere else
// (main Cloudflare build and dev server, via the `define` in vite.config.ts).
declare const __STATIC_SPA__: boolean;
